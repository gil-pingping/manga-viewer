# 배포와 팀 공유 (Cloudflare)

주소: `https://manga-viewer.giyun.workers.dev`

앱(정적)과 프록시(`/api`)가 **한 Worker, 한 오리진**에서 나온다. 오리진이 하나여야
IndexedDB 서재가 갈리지 않고 서비스워커가 켜진다.

## 배포

```sh
npm test && npm run deploy     # vite build + wrangler deploy
```

되돌리기: `npx wrangler rollback`

접속 토큰 교체:

```sh
npx wrangler secret put MV_TOKEN
```

`MV_TOKEN` 이 없으면 모든 `/api` 가 503 으로 닫힌다. 실수로 시크릿 없이 배포했을 때
"누구나 쓰는 프록시"가 되는 것보다 "아무도 못 쓰는 프록시"가 낫다.

## 접속 방법

처음 한 번만 토큰이 붙은 주소로 연다:

```
https://manga-viewer.giyun.workers.dev/?t=<토큰>
```

토큰은 즉시 쿠키(HttpOnly·Secure)로 바뀌고 **주소에서 지워진다**. 그 뒤로는 그냥
`https://manga-viewer.giyun.workers.dev` 로 들어가면 된다. 크롬 메뉴 → **홈 화면에 추가**
하면 아이콘으로 뜨고 전체화면으로 열린다.

왜 쿠키인가: `<img src>` 는 헤더를 못 붙인다. 토큰을 이미지 주소에 넣으면 **그 주소가
서재의 키**라서, 토큰을 바꾸는 순간 담아둔 화를 전부 못 찾게 된다.

## 팀원에게 열어주기 — 셋 중 하나

**서재는 각자 브라우저 안에 있다.** 서버에 사용자 데이터가 없으므로 계정·DB·권한 분리
같은 건 필요 없다. 정해야 할 것은 "누가 프록시를 쓸 수 있나"뿐이다.

### 1. 토큰 하나를 같이 쓴다 — 지금 바로 됨, 코드 0줄

`?t=<토큰>` 주소를 팀원에게 준다. 끝.

- 좋은 점: 추가 작업이 없다. 서로의 서재는 어차피 분리돼 있다.
- 한계: **한 명만 빼는 게 안 된다.** 토큰을 바꾸면 전원이 다시 로그인해야 한다.
- 적정 규모: 서로 믿는 2~4명.

### 2. Cloudflare Access (Zero Trust) — 사람이 늘거나 나갈 수 있으면 이쪽

Cloudflare 대시보드에서 이 앱을 Access 뒤에 두고 허용할 이메일을 넣는다. 구글 로그인이나
이메일 일회용 코드로 본인 확인이 된다. **무료 플랜이 50명까지** 커버한다.

- 좋은 점: 코드를 안 고친다. 사람별로 넣고 뺄 수 있고, 누가 언제 들어왔는지 로그가 남는다.
- 주의: 세션이 만료되면 로그인 화면으로 튄다. 담아둔 화는 그대로 읽히지만(서재가
  브라우저 안에 있으니까) 새 화를 가져오려면 다시 로그인해야 한다.
- `MV_TOKEN` 은 그대로 둬도 되고(이중 잠금), Access 만 쓸 거면 팀원에게 토큰을 한 번
  알려주면 된다.

### 3. 사람마다 토큰을 따로 준다 — 코드 조금

`worker/index.js` 의 `authorize` 가 지금은 토큰 하나와 비교한다. 목록 비교로 바꾸면
사람별 발급·회수가 된다. 20줄 남짓. 관리 화면은 없으니 시크릿을 직접 고쳐야 한다.

**권장**: 지금 인원이면 1번. 팀원이 바뀔 여지가 있으면 2번. 3번은 1과 2 사이가 필요할 때만.

## 알아둘 것

- **무료 한도**: Worker 요청 10만/일. 이미지 한 장이 한 요청이다 — 100장짜리 한 화 =
  100요청. 여러 명이 읽어도 여유가 있지만 무한은 아니다. 정적 파일은 Worker 를 안 타게
  해뒀다 (`run_worker_first: ["/api/*"]`).
- **오프라인**: 담아둔 화는 서버가 죽어도 읽힌다. 앱 껍데기는 서비스워커가 들고 있고,
  한 번만 열어도 되도록 설치 시점에 번들까지 미리 담는다.
- **JS 로 이미지를 채우는 사이트**는 Worker 가 대신 열어볼 수 없다(브라우저가 없다).
  그런 사이트는 불러오기 창의 **북마클릿**을 쓴다.
- **로그인이 필요한 페이지**도 서버가 대신 못 연다. 북마클릿만 가능하다.
- 맥 없이 태블릿에서만 돌리는 대안은 `TABLET.md` (Termux). 둘은 공존한다.
