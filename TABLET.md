# 태블릿에서 단독으로 쓰기 (안드로이드 + Termux)

목표: 맥북 없이 태블릿만으로 읽고, 새 화도 가져온다. 맥북은 코드를 고치는 곳으로만 남는다.

## 규칙 하나만 지킬 것 — 항상 `http://localhost:4173`

태블릿에서 앱을 열 때 **자기 자신의 localhost 만** 쓴다. 맥의 LAN 주소(`http://192.168.x.x:5173`)로
열지 않는다.

이유: IndexedDB 는 **오리진마다 별개**다. 두 주소를 섞어 쓰면 서재가 둘로 갈려서 담아둔 화가
사라진 것처럼 보인다. 게다가 서비스워커는 `localhost` 나 `https` 에서만 켜지므로 LAN 주소로는
앱을 오프라인에서 열 수 없다.

`npm run serve` 는 그래서 일부러 localhost 만 연다 (`vite.config.js` 의 `preview.host`).

## 1) Termux 준비

Termux 는 **F-Droid 나 GitHub 릴리스**에서 받는다. 구글 플레이 버전은 오래돼서 쓰지 않는다.

```sh
pkg update && pkg upgrade
pkg install nodejs git
node -v            # 20 이상이면 된다
```

## 2) 코드 가져오기

맥의 저장소를 태블릿으로 옮긴다. 방법은 아래 「최신으로 유지하기」 참고.

```sh
cd ~
git clone <맥에서 정한 주소> manga-viewer
cd manga-viewer
npm ci             # vite 등 빌드 도구까지 설치한다
```

`npm ci` 가 rollup/esbuild 네이티브 바이너리에서 실패하면 (안드로이드 arm64 빌드가 있어서
보통은 된다) 맥에서 `npm run build` 한 `dist/` 를 그대로 복사해 오고 태블릿에서는
`npx vite preview --port 4173` 만 돌려도 된다.

## 3) 띄우기

```sh
termux-wake-lock   # 안드로이드가 백그라운드에서 죽이지 않게
npm run serve      # 빌드 + localhost:4173
```

태블릿 크롬에서 `http://localhost:4173` → 메뉴 → **홈 화면에 추가**.
그러면 아이콘으로 뜨고 전체화면으로 열린다.

서버를 끄면? **담아둔 화는 그대로 읽힌다.** 서비스워커가 앱 껍데기를 들고 있고 이미지는
IndexedDB 에 있다. 서버는 **새 화를 가져올 때만** 필요하다.

## 4) 쓰는 법

- **주소 붙여넣기**가 주력. 회차 페이지 주소 하나면 된다.
- 목록 화면의 **「10화 담기」** — 지금 화부터 다음 화 방향으로 연달아 받아 서재에 넣는다.
  다음 화 주소가 끊기면 몇 화까지 담았는지 알려주고 멈춘다.
- **이미지를 JS 로 채우는 사이트**는 서버가 대신 열어봐야 하는데, Termux 에는 Chrome 을
  깔 수 없어서 그 경로가 없다. 대신 **북마클릿**을 쓴다 — 불러오기 창에서 주소를 복사해
  크롬 북마크의 **주소 칸**에 붙여넣고, 만화를 열어둔 채 그 북마크를 누른다.
  (태블릿에서는 `localhost` 가 곧 자기 자신이라 북마클릿 인수인계가 그대로 동작한다.)

## 최신으로 유지하기 (맥에서 고친 것을 태블릿으로)

맥에서 계속 고치고 태블릿은 `git pull` 로 따라간다. 전달 경로는 셋 중 하나 — **아직 고르지 않았다.**

| 방법 | 태블릿에서 | 필요한 것 |
|---|---|---|
| GitHub 비공개 저장소 | `git pull` | 맥에서 한 번 push. 와이파이 밖에서도 된다 |
| 맥에서 `git daemon` (LAN 전용) | `git pull` | 맥에서 켤 때마다 명령 하나. 코드가 외부로 안 나간다 |
| `git bundle` 파일 전달 | `git pull ~/mv.bundle` | 서버 없음. 파일을 옮기는 수고 |

받은 뒤:

```sh
git pull && npm ci && npm run serve
```

## 안 되는 것 (알고 있을 것)

- **프록시 없이 새 화 가져오기는 불가능하다.** 이미지 호스트가 Referer 를 검사한다
  (실측: 없음 403 · 다른 오리진 403 · 원본 페이지 200). CORS 헤더도 없어서 브라우저 혼자서는
  바이트를 읽을 수 없다. 그래서 태블릿에 서버가 필요하다 — 이것만은 우회가 없다.
- **헤드리스 렌더링**(`/api/render-page`)은 태블릿에서 안 된다. Chrome 이 없다. 북마클릿으로 대체.
- **로그인이 필요한 페이지**는 서버가 대신 열 수 없다 (세션이 없다). 북마클릿만 가능하다.
