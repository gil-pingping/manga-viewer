# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 도구 상태 (커밋 대상 아님)
.claude-flow
.playwright-mcp

# 기기 데이터 백업 (로컬 보관)
backups/

# Cloudflare
# .dev.vars 는 로컬 시크릿(MV_TOKEN)이다. 커밋되면 프록시 토큰이 이력에 영구히 남는다.
.dev.vars
.dev.vars.*
.wrangler

# 빌드가 키체인에서 생성 — 토큰 포함, 커밋 금지
src/generated/
